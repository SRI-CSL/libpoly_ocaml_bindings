(test
 (name test)
 (libraries ctypes sexplib containers libpoly)
 (flags (:standard -cclib -lpoly -ccopt "$LDFLAGS"))
 (preprocess (pps ppx_deriving.std))
)

(env (dev (flags (:standard -warn-error -A))))
